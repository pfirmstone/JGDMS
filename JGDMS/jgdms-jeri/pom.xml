<?xml version="1.0" encoding="UTF-8"?>
<!--
~ Copyright (C) 2014 the original author or authors.
~
~ Licensed under the Apache License, Version 2.0 (the "License");
~ you may not use this file except in compliance with the License.
~ You may obtain a copy of the License at
~
~ http://www.apache.org/licenses/LICENSE-2.0
~
~ Unless required by applicable law or agreed to in writing, software
~ distributed under the License is distributed on an "AS IS" BASIS,
~ WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
~ See the License for the specific language governing permissions and
~ limitations under the License.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>au.net.zeus</groupId>
        <artifactId>jgdms</artifactId>
        <version>4.0.0-SNAPSHOT</version>
    </parent>

    <groupId>au.net.zeus.jgdms</groupId>
    <artifactId>jgdms-jeri</artifactId>
    <packaging>jar</packaging>

    <name>Module :: JGDMS Jini Extensible Remote Invocation</name>
    <description>Apache JGDMS Remote Micro Services Platform
    </description>

    <properties>
        <high.scale.lib.version>1.0.6</high.scale.lib.version>
    </properties>

    <dependencies>  
        <dependency>
            <groupId>org.osgi</groupId>
            <artifactId>osgi.annotation</artifactId>
            <version>${osgi.annotation.version}</version>
            <scope>compile</scope>
        </dependency>
         <dependency>
            <groupId>au.net.zeus.jgdms</groupId>
            <artifactId>jgdms-platform</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>au.net.zeus.jgdms</groupId>
            <artifactId>jgdms-der</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>au.net.zeus.jgdms</groupId>
            <artifactId>jgdms-activation-parameters</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
          <groupId>com.boundary</groupId>
          <artifactId>high-scale-lib</artifactId>
          <version>${high.scale.lib.version}</version>
        </dependency>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.13.1</version>
            <scope>test</scope>
        </dependency>
        <!--<dependency>
            <groupId>bouncycastle</groupId>
            <artifactId>bcprov-jdk14</artifactId>
            <version>132</version>
            <type>jar</type>
        </dependency>-->
    </dependencies>

    <build>
        <plugins>  
            <plugin>
              <groupId>biz.aQute.bnd</groupId>
              <artifactId>bnd-maven-plugin</artifactId>
              <configuration>
                    <archive>                
                        <manifestEntries>
                                <Implementation-Version>${project.version}</Implementation-Version>
                            <Class-Path>jgdms-resources-${project.version}.jar high-scale-lib-${high.scale.lib.version}.jar</Class-Path>
                        </manifestEntries>
                    </archive>
              </configuration>
              <executions>
                    <execution>
                        <goals>
                            <goal>bnd-process</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin> 
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <executions>
                    <execution>
                        <id>attach-sources</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>jar-no-fork</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                      <release>21</release>
                        <debug>true</debug>
                        <encoding>UTF-8</encoding>
                        <meminitial>128m</meminitial>
                        <maxmem>1024m</maxmem>
                    </configuration>
            </plugin>
            <!--
              QuicEngineMtlsTest drives the internal jdk.internal.net.quic
              QUIC-TLS engine (JEP 517) and needs an add-exports of that internal
              package on the DirtyChai product image. It is EXCLUDED from the
              default build so a normal build (any JDK) is never broken; run it
              explicitly with the 'quic' profile below.
            -->
            <plugin>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>**/QuicEngineMtlsTest.java</exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <reporting>
        <plugins>
            <plugin>
                <artifactId>maven-javadoc-plugin</artifactId>
                <configuration>
                  <!--
                    <additionalDependencies>
                        <additionalDependency>
                          <groupId>au.net.zeus.jgdms</groupId>
                          <artifactId>jgdms-lib</artifactId>
                          <version>${project.version}</version>
                        </additionalDependency>
                        <additionalDependency>
                          <groupId>au.net.zeus.jgdms</groupId>
                          <artifactId>jgdms-lib-dl</artifactId>
                          <version>${project.version}</version>
                        </additionalDependency>
                    </additionalDependencies>
-->
                </configuration>
            </plugin>
            <plugin>
                <groupId>com.github.spotbugs</groupId>
                <artifactId>spotbugs-maven-plugin</artifactId>
                <configuration>
                    <excludeFilterFile>findbugs-exclude.xml</excludeFilterFile>
                </configuration>
            </plugin>
        </plugins>
    </reporting>

    <!--
      Profile activated when running under DirtyChai JDK with SecurityManager
      re-enabled.  Pass -Pdirtychai on the Maven command line (or set the
      dirtychai property) to run SPIFFE unit tests with SecurityManager active.
      This profile is only useful with a JDK that supports SecurityManager
      (e.g. DirtyChai); it has no effect on standard JDK 17+ builds.
    -->
    <profiles>
        <!--
          Runs ONLY QuicEngineMtlsTest (ADVICE item D: QUIC-engine-pair mTLS/SPIFFE),
          supplying the internal-package export the QUIC-TLS engine needs. Requires
          the DirtyChai product image (JEP 517 QUIC-TLS). Usage:
            mvn -o -pl jgdms-jeri test -Pquic
        -->
        <profile>
            <id>quic</id>
            <build>
                <plugins>
                    <plugin>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <configuration>
                            <argLine>--add-exports java.base/jdk.internal.net.quic=ALL-UNNAMED</argLine>
                            <excludes>
                                <exclude>none</exclude>
                            </excludes>
                            <includes>
                                <include>**/QuicEngineMtlsTest.java</include>
                            </includes>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>dirtychai</id>
            <build>
                <plugins>
                    <plugin>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <configuration>
                            <argLine>
                                -Djava.security.manager=default
                                -Djava.security.policy=${project.build.testOutputDirectory}/test.policy
                            </argLine>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
